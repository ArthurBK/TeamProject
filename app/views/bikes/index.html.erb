<div class ="text-center">
  <div class="container">
    <div class="row">
      <div class="col-sm-8 col-sm-offset-2">

        <% @bikes.each do |bike|%>
        <article>
          <%= link_to bike_path(bike) do %>
          <h4><%= bike.name %></h4>
          <% end %>
        </article>
        <% end %>

      </div>
    </div>
  </div>
</div>
