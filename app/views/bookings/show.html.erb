<div class="container">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2">

      <div class="text-center">
        <%= cl_image_tag @booking.bike.photo, height: 300, width: 200, radius: 20, crop: :fit %>
        <h2><%= @booking.bike.brand %> <%= @booking.bike.model %></h2>
        <p>Checkin: <%= @booking.checkin %></p>
        <p>Checkout: <%= @booking.checkout %></p>
      </div>

    </div>
  </div>
</div>
